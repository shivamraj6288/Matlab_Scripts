s=load("points2D_Set1.mat");
x=s.x;
y=s.y;
scatter(x,y);
hold on
coef = polyfit(x, y, 1);
val = polyval(coef, x);
plot(x,val);
xlabel("X axis");
ylabel("Y axis");
title("plot for part b of q3");
legend("scatter plot","line graph");
hold off;